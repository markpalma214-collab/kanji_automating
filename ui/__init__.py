"""UI pages for the AI-powered features. Each module exposes a single
`build_page(app)` function that returns a ready-to-pack tk.Frame, following
the same construction style as the original kanji_app.py pages."""
